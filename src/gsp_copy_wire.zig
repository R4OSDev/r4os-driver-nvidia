// Driver-owned ring layout; the command encoder belongs to R4NV.
const encoder = @import("r4nv_copy");
pub const notify: u32 = 0xb80000 + 0x030000 + 0x90;
pub const userd_offset: usize = 8192;
pub const put_offset: usize = userd_offset + 0x8c;
pub const completion_offset: usize = userd_offset + 512;
pub const push_offset: usize = 4096;
pub const slot_bytes: usize = 256;
pub const capacity: usize = 16;
pub const Error = encoder.Error;
pub const words = encoder.words;
pub const Rows = encoder.Rows;
pub const Block = encoder.Block;
pub const Transfer = encoder.Transfer;
pub const Program = encoder.Program;
pub const inc = encoder.inc;
pub const extent = encoder.extent;
pub const encode = encoder.encode;
pub const encodeTransfer = encoder.encodeTransfer;
pub const logicalBytes = encoder.logicalBytes;
pub const slice = encoder.slice;
pub const entry = encoder.entry;
pub const entryWords = encoder.entryWords;
